function were_or_was(c)
{
	if(c == 1 || c == 0 ) return 'was';
	return 'were';
}

function checkHandleSessionToken(session){
	// var sessionToken = session;
	// fetch(`https://api.idatamedia.org/QuerySessionToken?SessionToken=${sessionToken}`)
	// .then(res => res.json())
	// .then(result => {
	// 	if(result.Result == undefined){
			
	// 	}
	// })
	// .catch(e => console.warn("Error sessionToken Call: ", e));

	// return sessionToken;
}